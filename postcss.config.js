module.exports = {
    plugins: {
      // Injects `font-display` into all @font-face rules
      'postcss-font-display': { display: 'swap' },
      tailwindcss: {},
      autoprefixer: {},
    },
  };